<div class="chartplugin-container pb-5" style="background-color: #f4f7f6; min-height: 100vh; padding: 20px;">
    
    {{! HEADER NAVIGATION }}
    <div class="row mb-4">
        <div class="col-12">
            <div class="alert alert-info d-flex justify-content-between align-items-center shadow-sm" style="border-radius: 15px; border-left: 6px solid #007bff;">
                <div>
                    <span class="mr-3"><strong>Credits:</strong> <span class="badge badge-pill badge-primary">{{current_credits}}</span></span>
                    <span><strong>Status:</strong> <small class="text-muted">{{today_label}}</small></span>
                </div>
                <div>
                    {{#is_admin}}
                        <a href="manage.php" class="btn btn-dark btn-sm rounded-pill px-3 mr-2"><i class="fa fa-cog"></i> Admin</a>
                    {{/is_admin}}
                    <a href="buy.php" class="btn btn-success btn-sm rounded-pill px-3">Add Credits</a>
                </div>
            </div>
        </div>
    </div>

    {{! MAIN ROW: CHART & AI MINER }}
    <div class="row">
        <div class="col-lg-8 mb-4">
            <div class="card shadow-sm border-0 h-100" style="border-radius: 20px; overflow: hidden;">
                <div class="card-header bg-white border-0 pt-4 px-4 d-flex justify-content-between align-items-center">
                    <div>
                        <h4 class="font-weight-bold mb-0 text-dark">{{chart_title}}</h4>
                        <p class="small text-muted">{{chart_tooltip}}</p>
                    </div>
                    {{! RESTORED TOGGLE BUTTONS }}
                    <div class="btn-group shadow-sm" style="border-radius: 10px; overflow: hidden;">
                        <a href="?type={{current_type}}&format=bar" class="btn btn-sm btn-outline-primary {{#is_bar}}active{{/is_bar}}">Bar</a>
                        <a href="?type={{current_type}}&format=line" class="btn btn-sm btn-outline-primary {{#is_line}}active{{/is_line}}">Line</a>
                        <a href="?type={{current_type}}&format=pie" class="btn btn-sm btn-outline-primary {{#is_pie}}active{{/is_pie}}">Pie</a>
                    </div>
                </div>
                <div class="card-body" style="min-height: 450px;">
                    {{{main_chart_html}}}
                </div>
            </div>
        </div>

        {{! AI MINER PANEL }}
        <div class="col-lg-4 mb-4">
            <div class="card shadow-sm h-100 border-0" style="border-radius: 20px; background: linear-gradient(to bottom, #ffffff, #f0f7ff);">
                <div class="card-header bg-primary text-white text-center py-3" style="border-radius: 20px 20px 0 0;">
                    <strong><i class="fa fa-microchip mr-2"></i>AI PREDICTIVE MINER</strong>
                </div>
                <div class="card-body d-flex flex-column justify-content-center">
                    {{#is_enterprise}}
                        <div class="ai-chat-interface p-3 bg-white rounded shadow-sm mb-3" style="height: 300px; border: 1px solid #e3e6f0;">
                            <p class="small text-info text-center">AI System Online</p>
                        </div>
                        <div class="input-group">
                            <button class="btn btn-outline-primary rounded-circle mr-2"><i class="fa fa-microphone"></i></button>
                            <input type="text" class="form-control rounded-pill" placeholder="Analyze patterns...">
                        </div>
                    {{/is_enterprise}}
                    {{^is_enterprise}}
                        <div class="text-center p-4">
                            <i class="fa fa-lock fa-4x text-muted mb-3"></i>
                            <h5>Insights Restricted</h5>
                            <p class="text-muted small">The Predictive Miner requires an Enterprise license.</p>
                            <a href="buy.php" class="btn btn-primary btn-block rounded-pill font-weight-bold shadow">UPGRADE NOW</a>
                        </div>
                    {{/is_enterprise}}
                </div>
            </div>
        </div>
    </div>

    {{! SECOND ROW: SNAPSHOTS & PERFORMANCE BOOST }}
    <div class="row">
        <div class="col-md-4 mb-3">
            <div class="card shadow-sm border-0 p-3 h-100" style="border-radius: 20px;">
                <h6 class="text-primary font-weight-bold">LAST ANALYSIS</h6>
                <div class="chart-snapshot" style="height: 200px;">{{{last_viewed.html}}}</div>
            </div>
        </div>
        <div class="col-md-4 mb-3">
            <div class="card shadow-sm border-0 p-3 h-100" style="border-radius: 20px;">
                <h6 class="text-primary font-weight-bold">PREVIOUS VIEW</h6>
                <div class="chart-snapshot" style="height: 200px;">{{{prev_viewed.html}}}</div>
            </div>
        </div>
        <div class="col-md-4 mb-3">
            {{! PERFORMANCE BOOST BOX }}
            <div class="card shadow-sm border-0 h-100 text-center p-4" style="border-radius: 20px; background: linear-gradient(135deg, #1cc88a, #13855c); color: white;">
                <i class="fa fa-bolt fa-3x mb-3"></i>
                <h5 class="font-weight-bold">Performance Boost</h5>
                <p class="small mb-4 text-white-50">Generate an AI-driven learning path instantly.</p>
                <a href="process_boost.php?sesskey={{sesskey}}" class="btn btn-light btn-block rounded-pill text-success font-weight-bold">BOOST NOW</a>
            </div>
        </div>
    </div>
</div>